pipeline {
    agent { label 'linux' }
    tools {
        maven 'Maven 3.0.5'
    }
    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/elenaandries/weather.git'
            }
        }
        stage('Build') {
            steps {
                       
            }
        }
    }
}
